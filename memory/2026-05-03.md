# 2026-05-03

- `sajux_deploy/report/index.html`: head의 `__SAJUX_PARAMS__` 주석은 "하단 window.load에서 자동실행"이라고 되어 있었으나, 실제 하단 `<script>` 블록이 없어 URL 파라미터로 열 때 `runAnalysis`가 호출되지 않을 수 있음. `saju.html`과 동일한 `window.load` 자동 실행 IIFE를 `</body>` 직전에 추가함.
- **로드 오류(누락: runAnalysis, ANIMAL_SPRITE, BRANCH_HIDDEN) 근본 원인:** `function gb` 한 줄에서 `'❌ 흉'` 문자열이 `'❌ 흉``}` 로 백틱 오타 → 메인 인라인 스크립트 전체 SyntaxError로 중단 → 이후 전역 정의가 전부 스킵됨. `index.html` / `saju.html` / `view.html` 동일 위치를 `'❌ 흉';}` 형태로 수정.
- **추가 SyntaxError:** 대운 `strat` 삼항에서 템플릿 리터럴을 `` ` `` 로 열고 문자열처럼 `.'` 로 닫음 → `node --check`로 확인 후, 끝을 백틱(`` ` ``)으로 닫도록 세 파일 동일 수정.
