# 2026-04-20 Daily Memory

## 세션 요약 (04:00~05:38 UTC)

### 주요 작업
- X-SAJU_MASTER.html 렌더링 버그 집중 디버깅
- "사주원국 불러오는중" placeholder가 분석 후에도 사라지지 않는 문제
- 근본 원인: rAF(requestAnimationFrame) 비동기 타이밍 문제 → 동기 실행으로 교체
- showLoading 함수: rAF×2 → setTimeout(100ms) 후 동기 콜백으로 교체
- report-container에 대운/세운/월운 정적 기본 텍스트 삽입
- 각 inline-summary div에 풍부한 기본 풀이 텍스트 삽입 (JS 실패해도 보임)

### 마스터 최신 요구사항 (05:28 UTC)
1. **대운/세운/월운**: 보이는 것 전부 상세 설명 (각 대운 10년마다, 세운 각 연도, 월운 12개월 전부)
2. **만세력 바로 아래**: 유년기 → 초년기 → 중년기 → 말년기 인생 전반 서술
3. 텍스트 볼륨이 대가 PDF(페이지당 500~800자) 수준이어야 함

### 현재 파일 상태
- `/home/node/.openclaw/workspace/X-SAJU_MASTER.html` — 258KB 이상
- 배포: GitHub Pages via `deploy_now.py` (jediii88/X-SAJU 레포)
- `expand_all_content.py`, `expand_daeun_text.py`, `mega_expand.py` 작성 중

### 다음 할 일
- buildChapter1_Basic() 아래 유년~말년 인생 흐름 서술 추가
- buildDaewunLoop() 각 대운마다 500~800자 풀이
- buildSewunSection() 각 세운 연도마다 300~500자
- buildWolunSection() 각 월운마다 200~300자
- 모든 py 스크립트 실행 → 배포

### 주의사항
- AXE_PROTOCOL: 기존 코드 절대 삭제 금지, append/replace만
- window.SAJU_DB, window.SHINSAL_DESC, window.UNSUNG_MAP (window 할당 필수)
- 신살 이름: 한글만 (한자 괄호 제거)
- "Axe" 고객 화면 노출 절대 금지
# 2026-04-20 Daily Memory

## 세션 요약 (04:00~05:38 UTC)

### 주요 작업
- X-SAJU_MASTER.html 렌더링 버그 집중 디버깅
- "사주원국 불러오는중" placeholder가 분석 후에도 사라지지 않는 문제
- 근본 원인: rAF(requestAnimationFrame) 비동기 타이밍 문제 → 동기 실행으로 교체
- showLoading 함수: rAF×2 → setTimeout(100ms) 후 동기 콜백으로 교체
- report-container에 대운/세운/월운 정적 기본 텍스트 삽입
- 각 inline-summary div에 풍부한 기본 풀이 텍스트 삽입 (JS 실패해도 보임)

### 마스터 최신 요구사항 (05:28 UTC)
1. **대운/세운/월운**: 보이는 것 전부 상세 설명 (각 대운 10년마다, 세운 각 연도, 월운 12개월 전부)
2. **만세력 바로 아래**: 유년기 → 초년기 → 중년기 → 말년기 인생 전반 서술
3. 텍스트 볼륨이 대가 PDF(페이지당 500~800자) 수준이어야 함

### 현재 파일 상태
- `/home/node/.openclaw/workspace/X-SAJU_MASTER.html` — 258KB 이상
- 배포: GitHub Pages via `deploy_now.py` (jediii88/X-SAJU 레포)
- `expand_all_content.py`, `expand_daeun_text.py`, `mega_expand.py` 작성 중

### 다음 할 일
- buildChapter1_Basic() 아래 유년~말년 인생 흐름 서술 추가
- buildDaewunLoop() 각 대운마다 500~800자 풀이
- buildSewunSection() 각 세운 연도마다 300~500자
- buildWolunSection() 각 월운마다 200~300자
- 모든 py 스크립트 실행 → 배포

### 주의사항
- AXE_PROTOCOL: 기존 코드 절대 삭제 금지, append/replace만
- window.SAJU_DB, window.SHINSAL_DESC, window.UNSUNG_MAP (window 할당 필수)
- 신살 이름: 한글만 (한자 괄호 제거)
- "Axe" 고객 화면 노출 절대 금지

---

## 세션 요약 (05:38~07:13 UTC)

### 핵심 버그: fix_hanja2.py 후유증
- `buildRelationLines()` 함수: 지지 비교가 한글(`'자','축'`)로 되어 있어 한자 지지(`'子','丑'`)와 매칭 실패
  → 합충형파해 표가 완전히 빈 상태였음
- `BRANCH_HIDDEN`, `SHINSAL_LOGIC` map 값들도 한글로 오염되어 신살/지장간 오작동
- 복구: `buildRelationLines()` 전체를 한자 지지 기준으로 재작성, 방합 4종 전부 추가
- `buildChapter1_Basic` 함수 선언이 통째로 사라짐
  → JS 전체 파싱 오류 → 입력창에서 풀이로 전혀 넘어가지 않는 증상
  → `fix_ch1.py`로 함수 선언 복구

### 마스터 요청 (06:53 UTC)
- **인생 전반 서술 섹션 볼륨 확장** (스크린샷 첨부)
- 현재: 각 주별 3~4줄 고정 텍스트
- 목표: 천간 설명 + 지지 설명 + 인생 시기 서론 + 핵심 조언 카드 구조로 확장
- `expand_lifecycle.py` 작성 및 적용

### 현재 구현 상태
- 인생 전반 서술: 년주/월주/일주/시주 각각 천간·지지 2열 카드 + 서론 + 조언
- 각 10간 개별 설명 DB (STEM_CHAR), 각 12지 개별 설명 DB (BRANCH_DESC) 추가
- 시기 맥락 (PILLAR_CTX): 인생 시기, 궁(宮) 이름, 서론, 핵심 조언 각 주별 커스텀

### 마스터 요청 (07:05 UTC)
- "고객정보입력창에서 사주풀이로 안넘어가져"
- 원인: `buildChapter1_Basic` 함수 선언 누락 → JS 파싱 오류
- `fix_ch1.py`로 함수 선언 `function buildChapter1_Basic(data) {` 복구 완료

### 중요 교훈
- **fix_hanja2.py 같은 대규모 텍스트 치환 스크립트는 JS 함수 구조를 깨뜨릴 수 있음**
- 한자 치환 후 반드시 `node automated_qa.js`로 파싱 검증 필수
- 함수 선언이 누락되면 JS 전체가 죽음 — 증상은 "버튼 클릭 무반응"
- `buildRelationLines` 같이 문자열 비교 로직은 반드시 데이터 인코딩(한자 vs 한글)과 일치해야 함

### 배포 현황
- jediii88/X-SAJU GitHub Pages → sajux.com 반영
- QA: `node /home/node/.openclaw/workspace/automated_qa.js`
- 배포: `python3 /home/node/.openclaw/workspace/deploy_now.py`

### 남은 작업 (다음 세션)
- 브라우저에서 실제 렌더링 확인 (합충형파해 표, 인생 전반 서술 볼륨)
- 월운: 현재 달(4월)부터 12개월 순서 표시 (wolunRows 수정 필요)
- 대운 120세까지 표시 (`daeunData = yun.getDaYun().slice(1, 9)` → 슬라이스 확장 필요)
- `mega_expand.py` 실행 (Ch7 지장간, Ch9 개운법 확장)
- 잔존 한자 추가 검수
